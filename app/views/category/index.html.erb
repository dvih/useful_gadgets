<body>
  <h1>Products</h1>
  <p>
    <% @categories.each do |category| %>
      <div class="category">
        <h2><%=link_to category.name, show_path(category.id)%></h2>
          <%category.products.each do |product|%>
          <div class="product">
            <h3><%=product.name%></h3>
            <p class="product_image"><%= image_tag(product.image.url(:med)) %></p>
            <p>Price: $<%= product.price%></p>
          </div>
          <%end%>
      </div>
    <%end%>
  </p>
</body>
